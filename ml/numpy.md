## “еори€

numpy - библиотека дл€ научных расчетов. »спользуетс€ дл€ создани€ n-мерных массивов
оси начинаютс€ с 0 с левого верхнего угла

## ќперации

| ѕример | ќписание |
|:-----------|:--|
|`nums_array = np.array([1, 2, 3, 4])`|создать одномерный массив|
|`first = np.array([[1, 2, 3], [4, 5, 6]])`|создать двумерный массив|
|`first + second`|сложить поэлементно массивы|
|`first + 1`|увеличить элементы массива на 1|
|`first[1, 1]`|доступ к элементу массива по координатам|


## ћетоды

| ѕример | ќписание |
|:-----------|:--|
|`nums_array.shape`|вернет форму массива|
|`nums_array.ndim`|вернет размерность массива|
|`nums_array.size`|вернет размер массива|
|`nums_array.dtype`|вернет тип элементов массива|
|`nums_array.itemsize`|вернет занимаемую пам€ть элементом|
|`np.concatenate((first, second))`|сли€ние массивов по оси 0|
|`np.concatenate((first, second), axis=1)`|сли€ние массивов по оси 1|
|`first[1, 1]`|обратитьс€ к элементу массива по его положению|
|`np.zeros((2, 4), dtype=int)`|создаст массив из нулей указанной размерности|
|`np.ones((5, 3), dtype=int)`|создаст массив из единиц указанной размерности|
|`np.random.random((2, 4))`|создаст массив из случайных значений|
|`np.random.seed(0)`|установит сид дл€ генерации случайных чисел|
|`np.random.randint(10, 20, size=(2, 4))`|создает массив из целых чисел|
|`np.random.uniform(10.0, 50.0, size=(3, 5))`|рандомный массив из диапазона|
|`np.random.choice(np.array([1, 2, 3]), size=20)`|создает массив из одномерного массива size длины|
|`np.arange(start=-15, stop=30, step=3)`|создаст диапазон от до с шагом|
|`np.arange(20).reshape([4, 5])`|трансформирует массив, количество элементов должно совпадать|
|`np.random.randint(50, 80, (4, 3, 3)).flatten()`|преобразует массив в одномерный|
|`np.sum(revenue_pre_product)`|суммирует элементы массива|
|`np.sum(order_qty, axis=0)`|суммирует элементы массива по оси|
|`np.max(views)`|выводит максимальный элемент|
|`np.min(views)`|выводит минимальный элемент|
|`np.round(np.mean(views), 2)`|выводит среднее значение элементов массива с округлением до 2 знаком после зап€той|
|`np.split(order_ids, 3)`|разбивает массив на n одномерных массивов|
|`positive_ratings = ratings[ratings >= 4.0]`|создаст массив из массива где значени€ удовлетвор€ют условию|


## “ипы данных массивов

| ѕример | ќписание |
|:-----------|:--|
|`dtype='<U32'`|строки|
|`dtype('int64')`|целые числа|
|`dtype=object`|ссылки на объекты|
